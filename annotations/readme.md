MyPy is a tool for checking objects types,
     based on functions annotations.

To run MyPy 
     ` mypy <path> `

Example:
     ` mypy annotations/ `

Output:
     ` Success: no issues found in 1 source file `